# Copy this file to .env. .env is git-ignored.
# Optional — must match backend's GOOGLE_CLIENT_ID exactly (it's a public
# ID, not a secret). Leave blank to just hide the Google sign-in button.
VITE_GOOGLE_CLIENT_ID=
